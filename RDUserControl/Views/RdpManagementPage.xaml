<Page x:Class="RDUserControl.Views.RdpManagementPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:RDUserControl.Views"
      xmlns:viewmodels="clr-namespace:RDUserControl.ViewModels"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      d:DataContext="{d:DesignInstance Type=viewmodels:RdpManagementViewModel}"
      Title="RdpManagementPage">

    <Grid>
        <StackPanel Margin="10">
            <CheckBox Content="System RDP Enabled" IsChecked="{Binding SystemRdpEnabled}" />
            <DataGrid ItemsSource="{Binding Users}" AutoGenerateColumns="False" Margin="0,10,0,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Username" Binding="{Binding Username}" />
                    <DataGridTextColumn Header="Display Name" Binding="{Binding DisplayName}" />
                    <DataGridCheckBoxColumn Header="RDP Enabled" Binding="{Binding IsRdpEnabled}" />
                    <DataGridTemplateColumn Header="Actions">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Enable" Command="{Binding DataContext.EnableRdpForUserCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding Username}" />
                                    <Button Content="Disable" Command="{Binding DataContext.DisableRdpForUserCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding Username}" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
            <Button Content="Batch Enable RDP" Command="{Binding BatchEnableRdpCommand}" Margin="0,10,0,0" />
        </StackPanel>
    </Grid>
</Page>